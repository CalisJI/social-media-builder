# TikTok Default Template V2 Agent Pack

Copy this folder into the repository, preferably as:

```text
.openclaw/template-default-v2/
```

Then tell the Orchestrator to read:

```text
.openclaw/template-default-v2/task/TEMPLATE-DEFAULT-001.md
```

The visual reference is included under `references/`.
