# Vocabulary quiz v1

Package-only quiz format for `quiz-reveal-v1`: the prompt and guess window run from 0–4 seconds, then the Vietnamese meaning is revealed. It requires the `quizReveal` capability and does not alter legacy templates.
