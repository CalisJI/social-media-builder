# Vocabulary quiz v1

Package-only quiz format for `quiz-reveal-v1`: the prompt and configurable 3.4-second guess window run from 0–4 seconds, followed by the answer, an example and CTA. It requires the `quizReveal` capability and does not alter legacy templates.
